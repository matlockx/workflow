
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Node.js
node_modules/
coverage/
*.log
.npm/

# Local agent directory (created by opencode-init, not committed)
# This repo dogfoods its own workflows via opencode-init --backend=beads
.agent/
.opencode

# OpenCode
.agent/state/       # workflow runtime state (issues, tasks, counters)
